<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hrtfitnessapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HRTFitnessApp">
        <activity
            android:name=".NavScreen"
            android:label="@string/title_activity_nav_screen"
            android:theme="@style/Theme.HRTFitnessApp.NoActionBar"></activity>
        <activity android:name=".PostSurvey" />
        <activity android:name=".SurveyStart" />
        <activity android:name=".SurveyQuestion4" />
        <activity android:name=".SurveyQuestion3" />
        <activity android:name=".SurveyQuestion2" />
        <activity android:name=".SurveyQuestion1" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>